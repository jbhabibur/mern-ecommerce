export const TitleAndDescription = ({ title, description }) => (
  <div className="mb-6">
    <h1 className="text-3xl! uppercase font-bold! mb-2">{title}</h1>
    <p className="text-gray-500 text-sm md:text-base">{description}</p>
  </div>
);
